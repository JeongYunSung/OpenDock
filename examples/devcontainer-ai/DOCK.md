# Devcontainer AI

Install an AI-friendly Dev Container reference setup.

This dock adds a `.devcontainer/devcontainer.json` and a short guide describing
how to adapt it for a repository. It does not assume Docker is installed and
does not start a container.

Use it when contributors or AI agents need a reproducible workspace boundary
before running project tools.
