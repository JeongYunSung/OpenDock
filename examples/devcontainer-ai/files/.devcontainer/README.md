# Dev Container

This is a starter Dev Container definition for AI-assisted project work.

Before enforcing it:

- Confirm the base image matches the project runtime.
- Add only required editor extensions.
- Move project-specific setup into documented tasks.
- Keep secrets outside the container definition.
