opendock: 1
id: opendock/customer-support
summary: Support playbooks, FAQ structure, ticket triage, escalation, and reusable response prompts.
readme: DOCK.md
logo: logo.png
tags:
  - support
  - customer-success
  - faq
  - operations
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-customer-support/SKILL.md
    to: .agents/skills/opendock-customer-support/SKILL.md
  - from: files/.codex/skills/opendock-customer-support/SKILL.md
    to: .codex/skills/opendock-customer-support/SKILL.md
  - from: files/.claude/skills/opendock-customer-support/SKILL.md
    to: .claude/skills/opendock-customer-support/SKILL.md
  - from: files/.cursor/rules/opendock-customer-support.mdc
    to: .cursor/rules/opendock-customer-support.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/SUPPORT_PLAYBOOK.md
    to: SUPPORT_PLAYBOOK.md
  - from: files/FAQ.md
    to: FAQ.md
  - from: files/TICKET_TRIAGE.md
    to: TICKET_TRIAGE.md
  - from: files/ESCALATION.md
    to: ESCALATION.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-customer-support/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-customer-support/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-customer-support.mdc) { exit 0 } else { exit 1 }"
  - id: support-playbook-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath SUPPORT_PLAYBOOK.md) { exit 0 } else { exit 1 }"
  - id: faq-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath FAQ.md) { exit 0 } else { exit 1 }"
  - id: ticket-triage-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath TICKET_TRIAGE.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
