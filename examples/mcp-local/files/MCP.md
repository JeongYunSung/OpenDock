# MCP Setup Notes

These files are examples, not active credentials.

- Review every server before enabling it in an AI tool.
- Keep tokens in the tool's secret store or environment, not in committed files.
- Prefer read-only scopes for repository and documentation servers.
- Enable only the tools needed for the current project.
