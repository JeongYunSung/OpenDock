# Oh My Agent

Install Oh My Agent and run its interactive project bootstrap.

This dock checks for Bun, installs the `oh-my-agent` CLI, runs `oma` in the
project directory, and verifies the result with `oma doctor`.

Use it when a project should be configured through Oh My Agent without shipping
additional project file payloads in the dock.
