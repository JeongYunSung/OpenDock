opendock: 1
id: opendock/agent-ready
summary: Baseline instructions and shared rules for AI coding agents.
readme: DOCK.md
logo: logo.png
tags:
  - ai-agent
  - workspace
  - instructions
  - starter
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-agent-ready/SKILL.md
    to: .agents/skills/opendock-agent-ready/SKILL.md
  - from: files/.codex/skills/opendock-agent-ready/SKILL.md
    to: .codex/skills/opendock-agent-ready/SKILL.md
  - from: files/.claude/skills/opendock-agent-ready/SKILL.md
    to: .claude/skills/opendock-agent-ready/SKILL.md
  - from: files/.cursor/rules/opendock-agent-ready.mdc
    to: .cursor/rules/opendock-agent-ready.mdc
  - from: files/README.md
    to: README.md
  - from: files/CONVENTIONS.md
    to: CONVENTIONS.md
  - from: files/.github/copilot-instructions.md
    to: .github/copilot-instructions.md
  - from: files/.github/instructions/agent.instructions.md
    to: .github/instructions/agent.instructions.md
  - from: files/.github/prompts/review.prompt.md
    to: .github/prompts/review.prompt.md
  - from: files/.cursor/rules/project.mdc
    to: .cursor/rules/project.mdc
  - from: files/.claude/rules/project.md
    to: .claude/rules/project.md
  - from: files/.windsurf/rules/project.md
    to: .windsurf/rules/project.md
  - from: files/.clinerules/project.md
    to: .clinerules/project.md
  - from: files/.roo/rules/project.md
    to: .roo/rules/project.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-agent-ready/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-agent-ready/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-agent-ready.mdc) { exit 0 } else { exit 1 }"
  - id: copilot-instructions
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .github/copilot-instructions.md) { exit 0 } else { exit 1 }"
  - id: cursor-rules
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/project.mdc) { exit 0 } else { exit 1 }"
  - id: claude-rules
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/rules/project.md) { exit 0 } else { exit 1 }"
