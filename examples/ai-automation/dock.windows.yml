opendock: 1
id: opendock/ai-automation
summary: A practical workspace for mapping and designing safe internal automations.
readme: DOCK.md
logo: logo.png
tags:
  - automation
  - operations
  - mcp
  - workflow
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-ai-automation/SKILL.md
    to: .agents/skills/opendock-ai-automation/SKILL.md
  - from: files/.codex/skills/opendock-ai-automation/SKILL.md
    to: .codex/skills/opendock-ai-automation/SKILL.md
  - from: files/.claude/skills/opendock-ai-automation/SKILL.md
    to: .claude/skills/opendock-ai-automation/SKILL.md
  - from: files/.cursor/rules/opendock-ai-automation.mdc
    to: .cursor/rules/opendock-ai-automation.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/AUTOMATION_MAP.md
    to: AUTOMATION_MAP.md
  - from: files/WORKFLOW_LIBRARY.md
    to: WORKFLOW_LIBRARY.md
  - from: files/MCP_GUIDE.md
    to: MCP_GUIDE.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-ai-automation/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-ai-automation/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-ai-automation.mdc) { exit 0 } else { exit 1 }"
  - id: automation-map
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AUTOMATION_MAP.md) { exit 0 } else { exit 1 }"
  - id: workflow-library
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath WORKFLOW_LIBRARY.md) { exit 0 } else { exit 1 }"
  - id: mcp-guide
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath MCP_GUIDE.md) { exit 0 } else { exit 1 }"
  - id: prompts
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
