# Agent Rules

Install path-scoped rule files for multi-agent coding tools.

This dock adds a shared `AGENTS.md` block plus focused rules for GitHub Copilot,
Cursor, and Claude Code. The rules separate architecture, testing, and
TypeScript guidance so agents can load tighter context for the files they edit.

Use it when a repository already has basic instructions and needs more precise
agent behavior by file type or workflow.
