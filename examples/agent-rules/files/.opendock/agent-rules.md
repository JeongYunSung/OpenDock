# Agent Rules Harness

This project uses path-scoped AI instructions.

- `AGENTS.md` contains shared repository expectations.
- `.github/instructions/*.instructions.md` contains Copilot path rules.
- `.cursor/rules/*.mdc` contains Cursor project rules.
- `.claude/rules/*.md` contains Claude Code project rules.

Keep rules short, testable, and tied to real repository behavior.
