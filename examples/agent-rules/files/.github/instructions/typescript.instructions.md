---
applyTo: "**/*.ts,**/*.tsx"
---

# TypeScript Instructions

- Preserve existing public types unless the task explicitly changes an API.
- Prefer narrow types over broad `any` values.
- Keep runtime validation close to external input boundaries.
- Add focused tests for changed behavior.
