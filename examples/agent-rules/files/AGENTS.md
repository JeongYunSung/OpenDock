# Agent Rules

## Operating Model

- Read repository instructions before making changes.
- Prefer path-specific rules when they apply to the files being edited.
- Keep implementation changes scoped to the requested behavior.
- Report validation commands and skipped checks.
