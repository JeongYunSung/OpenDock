# Dev Env

Install a project-local developer environment baseline.

This dock adds a `mise.toml` with common runtime pins and repeatable tasks for
install, lint, test, build, and doctor. It does not install mise automatically.

Use it when a project needs a visible, editable place for tool versions and
standard validation commands before AI agents start work.
