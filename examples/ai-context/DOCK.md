# AI Context

Install a small context-packaging setup for AI coding sessions.

This dock adds a `repomix.config.json`, ignores generated context bundles, and
adds a reusable repository-summary prompt under `.opendock/prompts`.

Use it when a project needs repeatable, reviewable context exports for Codex,
Claude, Gemini, Copilot, or other AI tools.
