opendock: 1
summary: Job description, interview guide, scorecard, and candidate outreach workspace.
readme: DOCK.md
logo: logo.png
tags:
  - recruiting
  - hiring
  - interview
  - outreach
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-recruiter-ai/SKILL.md
    to: .agents/skills/opendock-recruiter-ai/SKILL.md
  - from: files/.codex/skills/opendock-recruiter-ai/SKILL.md
    to: .codex/skills/opendock-recruiter-ai/SKILL.md
  - from: files/.claude/skills/opendock-recruiter-ai/SKILL.md
    to: .claude/skills/opendock-recruiter-ai/SKILL.md
  - from: files/.cursor/rules/opendock-recruiter-ai.mdc
    to: .cursor/rules/opendock-recruiter-ai.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/JOB_DESCRIPTION.md
    to: JOB_DESCRIPTION.md
  - from: files/INTERVIEW_GUIDE.md
    to: INTERVIEW_GUIDE.md
  - from: files/SCORECARD.md
    to: SCORECARD.md
  - from: files/CANDIDATE_OUTREACH.md
    to: CANDIDATE_OUTREACH.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-recruiter-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-recruiter-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-recruiter-ai.mdc) { exit 0 } else { exit 1 }"
  - id: job-description-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath JOB_DESCRIPTION.md) { exit 0 } else { exit 1 }"
  - id: interview-guide-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath INTERVIEW_GUIDE.md) { exit 0 } else { exit 1 }"
  - id: scorecard-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath SCORECARD.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
