opendock: 1
summary: Campaign briefs, content calendars, SEO briefs, and ad experiment planning.
readme: DOCK.md
logo: logo.png
tags:
  - marketing
  - seo
  - campaign
  - content
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-marketer-ai/SKILL.md
    to: .agents/skills/opendock-marketer-ai/SKILL.md
  - from: files/.codex/skills/opendock-marketer-ai/SKILL.md
    to: .codex/skills/opendock-marketer-ai/SKILL.md
  - from: files/.claude/skills/opendock-marketer-ai/SKILL.md
    to: .claude/skills/opendock-marketer-ai/SKILL.md
  - from: files/.cursor/rules/opendock-marketer-ai.mdc
    to: .cursor/rules/opendock-marketer-ai.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/CAMPAIGN_BRIEF.md
    to: CAMPAIGN_BRIEF.md
  - from: files/CONTENT_CALENDAR.md
    to: CONTENT_CALENDAR.md
  - from: files/SEO_BRIEF.md
    to: SEO_BRIEF.md
  - from: files/AD_EXPERIMENTS.md
    to: AD_EXPERIMENTS.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-marketer-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-marketer-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-marketer-ai.mdc) { exit 0 } else { exit 1 }"
  - id: campaign-brief-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CAMPAIGN_BRIEF.md) { exit 0 } else { exit 1 }"
  - id: content-calendar-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CONTENT_CALENDAR.md) { exit 0 } else { exit 1 }"
  - id: seo-brief-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath SEO_BRIEF.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
