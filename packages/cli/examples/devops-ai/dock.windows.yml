opendock: 1
summary: CI/CD, deployment, incident, and secrets policy guidance for AI-assisted operations.
readme: DOCK.md
logo: logo.png
tags:
  - devops
  - ci-cd
  - deployment
  - incident
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-devops-ai/SKILL.md
    to: .agents/skills/opendock-devops-ai/SKILL.md
  - from: files/.codex/skills/opendock-devops-ai/SKILL.md
    to: .codex/skills/opendock-devops-ai/SKILL.md
  - from: files/.claude/skills/opendock-devops-ai/SKILL.md
    to: .claude/skills/opendock-devops-ai/SKILL.md
  - from: files/.cursor/rules/opendock-devops-ai.mdc
    to: .cursor/rules/opendock-devops-ai.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/CI_CD.md
    to: CI_CD.md
  - from: files/DEPLOYMENT_RUNBOOK.md
    to: DEPLOYMENT_RUNBOOK.md
  - from: files/INCIDENT_RUNBOOK.md
    to: INCIDENT_RUNBOOK.md
  - from: files/SECRETS_POLICY.md
    to: SECRETS_POLICY.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-devops-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-devops-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-devops-ai.mdc) { exit 0 } else { exit 1 }"
  - id: ci-cd-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CI_CD.md) { exit 0 } else { exit 1 }"
  - id: deployment-runbook-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath DEPLOYMENT_RUNBOOK.md) { exit 0 } else { exit 1 }"
  - id: incident-runbook-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath INCIDENT_RUNBOOK.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
