opendock: 1
summary: Mobile architecture, UI patterns, release readiness, and testing guidance.
readme: DOCK.md
logo: logo.png
tags:
  - mobile
  - ios
  - android
  - release
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-mobile-ai/SKILL.md
    to: .agents/skills/opendock-mobile-ai/SKILL.md
  - from: files/.codex/skills/opendock-mobile-ai/SKILL.md
    to: .codex/skills/opendock-mobile-ai/SKILL.md
  - from: files/.claude/skills/opendock-mobile-ai/SKILL.md
    to: .claude/skills/opendock-mobile-ai/SKILL.md
  - from: files/.cursor/rules/opendock-mobile-ai.mdc
    to: .cursor/rules/opendock-mobile-ai.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/MOBILE_ARCHITECTURE.md
    to: MOBILE_ARCHITECTURE.md
  - from: files/UI_PATTERNS.md
    to: UI_PATTERNS.md
  - from: files/RELEASE_CHECKLIST.md
    to: RELEASE_CHECKLIST.md
  - from: files/TESTING.md
    to: TESTING.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-mobile-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-mobile-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-mobile-ai.mdc) { exit 0 } else { exit 1 }"
  - id: mobile-architecture-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath MOBILE_ARCHITECTURE.md) { exit 0 } else { exit 1 }"
  - id: ui-patterns-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath UI_PATTERNS.md) { exit 0 } else { exit 1 }"
  - id: release-checklist-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath RELEASE_CHECKLIST.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
