opendock: 1
id: opendock/data-analyst
summary: Metric definitions, SQL review, dashboard briefs, and experiment analysis templates.
readme: DOCK.md
logo: logo.png
tags:
  - data
  - analytics
  - sql
  - dashboard
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-data-analyst/SKILL.md
    to: .agents/skills/opendock-data-analyst/SKILL.md
  - from: files/.codex/skills/opendock-data-analyst/SKILL.md
    to: .codex/skills/opendock-data-analyst/SKILL.md
  - from: files/.claude/skills/opendock-data-analyst/SKILL.md
    to: .claude/skills/opendock-data-analyst/SKILL.md
  - from: files/.cursor/rules/opendock-data-analyst.mdc
    to: .cursor/rules/opendock-data-analyst.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/METRICS_DICTIONARY.md
    to: METRICS_DICTIONARY.md
  - from: files/SQL_REVIEW.md
    to: SQL_REVIEW.md
  - from: files/DASHBOARD_BRIEF.md
    to: DASHBOARD_BRIEF.md
  - from: files/EXPERIMENT_ANALYSIS.md
    to: EXPERIMENT_ANALYSIS.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-data-analyst/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-data-analyst/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-data-analyst.mdc) { exit 0 } else { exit 1 }"
  - id: metrics-dictionary-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath METRICS_DICTIONARY.md) { exit 0 } else { exit 1 }"
  - id: sql-review-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath SQL_REVIEW.md) { exit 0 } else { exit 1 }"
  - id: dashboard-brief-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath DASHBOARD_BRIEF.md) { exit 0 } else { exit 1 }"
  - id: prompts-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
