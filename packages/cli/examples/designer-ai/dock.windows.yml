opendock: 1
summary: UI, UX, product design, design system, Figma workflow, and review templates.
readme: DOCK.md
logo: logo.png
tags:
  - design
  - ux
  - figma
  - product
files:
  - from: files/AGENTS.md
    to: AGENTS.md
  - from: files/CLAUDE.md
    to: CLAUDE.md
  - from: files/GEMINI.md
    to: GEMINI.md
  - from: files/.agents/skills/opendock-designer-ai/SKILL.md
    to: .agents/skills/opendock-designer-ai/SKILL.md
  - from: files/.codex/skills/opendock-designer-ai/SKILL.md
    to: .codex/skills/opendock-designer-ai/SKILL.md
  - from: files/.claude/skills/opendock-designer-ai/SKILL.md
    to: .claude/skills/opendock-designer-ai/SKILL.md
  - from: files/.cursor/rules/opendock-designer-ai.mdc
    to: .cursor/rules/opendock-designer-ai.mdc
  - from: files/PROMPTS.md
    to: PROMPTS.md
  - from: files/README.md
    to: README.md
  - from: files/DESIGN.md
    to: DESIGN.md
  - from: files/DESIGN_SYSTEM.md
    to: DESIGN_SYSTEM.md
  - from: files/UX_REVIEW.md
    to: UX_REVIEW.md
  - from: files/FIGMA_WORKFLOW.md
    to: FIGMA_WORKFLOW.md
doctor:
  - id: agents-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath AGENTS.md) { exit 0 } else { exit 1 }"
  - id: claude-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath CLAUDE.md) { exit 0 } else { exit 1 }"
  - id: gemini-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath GEMINI.md) { exit 0 } else { exit 1 }"
  - id: codex-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .codex/skills/opendock-designer-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: claude-skill
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .claude/skills/opendock-designer-ai/SKILL.md) { exit 0 } else { exit 1 }"
  - id: cursor-rule
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath .cursor/rules/opendock-designer-ai.mdc) { exit 0 } else { exit 1 }"
  - id: design-md
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath DESIGN.md) { exit 0 } else { exit 1 }"
  - id: design-system
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath DESIGN_SYSTEM.md) { exit 0 } else { exit 1 }"
  - id: ux-review
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath UX_REVIEW.md) { exit 0 } else { exit 1 }"
  - id: figma-workflow
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath FIGMA_WORKFLOW.md) { exit 0 } else { exit 1 }"
  - id: prompts
    check: powershell -NoProfile -NonInteractive -Command "if (Test-Path -LiteralPath PROMPTS.md) { exit 0 } else { exit 1 }"
